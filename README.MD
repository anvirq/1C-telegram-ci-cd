# 1C Telegram CI/CD Bot

Telegram бот для автоматизации CI/CD процессов в экосистеме 1С:Предприятие. Позволяет удаленно управлять обновлением конфигураций баз данных и настройкой RAS сервера через Telegram.

## 🚀 Быстрый старт

### 1. Настройка конфигурации

Откройте файл `.env` и настройте параметры:

```env
TELEGRAM_TOKEN=your_telegram_bot_token_here
INFOBASE_NAME=MyInfoBase
SERVER_HOST=localhost
ALLOWED_IDS=all
```

**Обязательные параметры:**
- `TELEGRAM_TOKEN` - Получите у @BotFather в Telegram
- `INFOBASE_NAME` - Имя вашей информационной базы 1С

### 2. Запуск бота

1cbot.exe

### 3. Остановка бота

stopbot.bat

Или используйте Ctrl+C в консоли где запущен бот.

## 🎯 Использование команд

### Обновление конфигурации базы данных

```
/updatedb <Пользователь> <Пароль>
```

**Пример:**
```
/updatedb admin mypassword123
```

**Что происходит:**
1. Блокировка информационной базы
2. Отключение всех активных сеансов
3. Обновление конфигурации через конфигуратор
4. Снятие блокировки

### Регистрация RAS сервера

```
/regras <Версия платформы>
```

**Пример:**
```
/regras 8.3.23.1810
```

**Что происходит:**
- Останавливается существующая служба RAS (если есть)
- Создается новая служба Windows
- Служба запускается в автоматическом режиme

## ⚙️ Конфигурация безопасности

### Ограничение доступа

По умолчанию бот доступен всем. Для ограничения доступа:

```env
ALLOWED_IDS=123456789,987654321
```

Где числа - ID пользователей Telegram (можно получить у @userinfobot)
